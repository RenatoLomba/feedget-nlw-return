export enum FeedbackTypes {
  BUG = 'BUG',
  IDEA = 'IDEA',
  OTHER = 'OTHER',
}

export enum ApplicationResultTypes {
  SUCCESS = 1,
  FAIL = 0,
}

export enum ErrorMessages {
  INVALID_SCREENSHOT_FORMAT = 'Invalid Screenshot format!',
}
